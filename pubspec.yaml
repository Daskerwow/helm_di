name: helm_di
description: A small, pure-Dart dependency container with scopes and disposal.
version: 0.1.0
repository: https://github.com/Daskerwow/helm_di.git

environment:
  sdk: ^3.13.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
